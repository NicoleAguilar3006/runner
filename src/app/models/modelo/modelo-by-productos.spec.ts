import { ModeloByProductos } from './modelo-by-productos';

describe('ModeloByProductos', () => {
  it('should create an instance', () => {
    expect(new ModeloByProductos()).toBeTruthy();
  });
});
